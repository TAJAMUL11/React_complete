function Inline(){

    const styles = {
        backgroundColor: "red",
        border: "none",
        padding: "1rem",
        fontSize: "1rem",
        color : "#fff",
        borderRadius: "10px",
        cursor : "pointer",
    }
    return (
        <button style = {styles}>Click Me</button>
    );
}
export default Inline;